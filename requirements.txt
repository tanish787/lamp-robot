# Version floors, not exact pins: these are the versions the project was
# developed and tested against, and the APIs used here (notably
# piper-tts >= 1.7's PiperVoice.synthesize_wav) are not present in older
# releases. Pin exactly if you need byte-reproducible environments.

# Body
pybullet>=3.2.7
websockets>=15.0
numpy>=2.0
pillow>=10.0
# Audio playback shells out to the system's `aplay` (alsa-utils, present by
# default on Ubuntu Desktop) rather than depending on `simpleaudio`, whose
# native bindings segfaulted reliably on real target hardware.

# Brain — perception
# mediapipe 1.0 removed the legacy `mp.solutions` API (face_detection,
# face_mesh, etc.) in favor of a new Tasks API with a different model-
# loading mechanism. brain/engagement.py uses the legacy API, so this is
# capped below 1.0 rather than left as an open floor (confirmed broken on
# real target hardware: `pip install -r requirements.txt` installed 1.0.1
# and MediaPipeFaceMonitor failed with "module mediapipe has no attribute
# 'solutions'"). Migrating to the Tasks API is a real option later, but
# needs a separately-downloaded .tflite model and a rewrite, not a pin.
mediapipe>=0.10.20,<1.0
opencv-python>=4.9
ultralytics>=8.3

# Brain — audio
sounddevice>=0.5
webrtcvad>=2.0.10
faster-whisper>=1.1
piper-tts>=1.7.0

# Brain — reasoning
llama-cpp-python>=0.3

# Tooling
pytest>=8.0
pytest-asyncio>=0.24
psutil>=6.0
